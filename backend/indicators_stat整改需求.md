# 现在出现了backend\indicators_stat.py两个需要进行优化的地方（都是需要修改原有json的数据结构）：
问题 1：添加详细数据字段
1、对于“六个核心指标分布对比”进行更加详细的展示（也就是json数据里面的indicator_statistic数组进行扩充）
之前展示的只是有关六个核心指标的平均值、中位数、标准差之类的总结数据，没有对应每个核心值的详细数据，就例如每个核心值有哪些数据项，他们这些数据项分别是多少
**原需求：**
- 在每个 `indicator_statistics` 中添加 `detail_data` 字段
- 包含所有项目的详细数据

**问题：**
- 会导致数据重复 6 次（6 个指标 × 281 条数据 = 1686 条记录）
- 数据冗余严重，浪费空间

**优化方案：**
- 采用**方案 1**：将详细数据提升到顶层
- 新增 `projects_detail` 字段，所有指标共用
- 数据只存储 1 次（281 条记录）
- **节省空间：83%** 🎉

**修改位置：**
- `backend/indicators_stat.py` 第 68 行：添加 `"projects_detail": []`
- `backend/indicators_stat.py` 第 74-83 行：填充 `projects_detail` 数据


### 问题 2：Top10 标准化值出现负数（已修复）

**问题描述：**
- 原代码使用 **Z-score 标准化**：`(value - mean) / std`
- 结果范围：**可以是负数**（低于平均值时）
- 与原版文件不一致

**原版逻辑：**
- 使用 **Min-Max 标准化**：`(value - min) / (max - min)`
- 结果范围：**0 到 1**，不会出现负数
- 参考：`backend/indicators_stat_org.py` 第 119 行

**修改位置：**
- `backend/indicators_stat.py` 第 101-124 行

**修改前：**
```python
# ❌ Z-score 标准化（会产生负数）
project_data["indicator_values"][f"{ind}_scaled"] = round(
    (row[ind] - df_valid[ind].mean()) / df_valid[ind].std(), 4
)
```

**修改后：**
```python
# ✅ Min-Max 标准化（0-1 范围）
values = top10_projects[ind].values
values_min = values.min()
values_max = values.max()
if values_max - values_min > 1e-8:
    scaled_value = (row[ind] - values_min) / (values_max - values_min)
else:
    scaled_value = 0.0

project_data["indicator_values"][f"{ind}_scaled"] = round(scaled_value, 4)
```